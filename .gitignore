# Datasets and training output: ~12 GB, all reproducible from ml/prepare.py
data/
artifacts/saved_model/
artifacts/model.keras
ml/train.log

# The trained model IS committed; it is ~5 MB and the app needs it in the APK.
!artifacts/crop_model.tflite
!artifacts/labels.json
!artifacts/labels.txt
!artifacts/metrics.json
!artifacts/confusion.csv

# Copies. content/ at the repo root is the source of truth.
mobile/assets/*.json
mobile/assets/*.tflite

node_modules/
.expo/
.expo-export/
mobile/android/
mobile/ios/

__pycache__/
*.pyc
backend/bahi.db
backend/*.db-wal
backend/*.db-shm

# Never commit these
.env
.kaggle/
*.key
